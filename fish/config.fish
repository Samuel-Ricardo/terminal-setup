if status is-interactive
    # Commands to run in interactive sessions can go here
end

#alias nvm "nvm.exe"

  alias ll "exa -l -g --icons --tree --level=2"
  alias la "ll -a"
  alias lst "exa -l -g --icons"
  alias lsta "lst -a"
